# ArgoCD Terraform Configuration Example
# Copy this file to terraform.tfvars and adjust values as needed

# Choose which cluster to deploy ArgoCD to:
# For K3D cluster:
kubeconfig_path        = "~/.kube/config"
cluster_type           = "k3d"

# For EKS cluster (uncomment and update):
# kubeconfig_path        = "~/.kube/eks-config"
# cluster_type           = "eks"

argocd_namespace       = "argocd"
argocd_chart_version   = "7.0.0"
argocd_domain          = "argocd.example.com"
git_repository_url     = "https://github.com/your-org/your-gitops-repo.git"
git_repository_branch  = "main"
git_repository_path    = "k8s-manifests"
enable_ingress         = false

